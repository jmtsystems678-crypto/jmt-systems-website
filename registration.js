const form = document.querySelector('#registration-form');
const queryProgram = new URLSearchParams(location.search).get('program');
if (queryProgram && [...document.querySelector('#program').options].some(option => option.value === queryProgram)) document.querySelector('#program').value = queryProgram;
form.addEventListener('submit', async event => { event.preventDefault(); const button = form.querySelector('button'); button.disabled = true; button.textContent = 'Opening secure checkout…'; const response = await fetch('/api/create-registration-payment', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ program: document.querySelector('#program').value, full_name: document.querySelector('#full-name').value, email: document.querySelector('#registration-email').value, phone: document.querySelector('#phone').value, notes: document.querySelector('#notes').value }) }); const result = await response.json(); if (!response.ok) { button.disabled = false; button.textContent = 'Try again'; document.querySelector('#confirmation').textContent = result.error || 'Unable to start checkout.'; return; } window.location.assign(result.authorization_url); });
